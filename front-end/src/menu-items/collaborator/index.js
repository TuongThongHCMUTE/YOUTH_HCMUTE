import dashboard from './dashboard';
import major from './major';

// ===========================|| MENU ITEMS ||=========================== //

const menuItems = {
    items: [dashboard, major]
};

export default menuItems;
